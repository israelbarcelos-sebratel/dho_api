spring.application.name=dho-api

# MariaDB DHO_Application Configuration
spring.datasource.url=jdbc:mariadb://${DB_HOST:10.0.11.171}:${DB_PORT:3306}/${DB_NAME:DHO_Application}
spring.datasource.username=${DB_USERNAME:dev_reading}
spring.datasource.password=${DB_PASSWORD:tMr44i2Vzfk#}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Server
server.port=8080


# Upload Configuration
upload.dir=${UPLOAD_DIR:uploads}
